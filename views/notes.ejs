<% include header.ejs %>

<p class="msg" style="display: none;"></p>

<!-- <div class="save">	<input id="save" type="submit" value="Save"/></div> -->

<input id="add" type="submit" value="+"/>

<% if(locals.notes) { 
	notes.forEach((note) => { %>
<textarea id="<%=note.id%>" class="note" <% if(note.x){ %> style="width:<%=note.x%>px; height:<%=note.y%>px" <% } %> ><%=note.note%></textarea>
<% }); } %>

<script>
	function getUser(){
		var user = <%-JSON.stringify(user)%>;
		return(user);
	}
  function getNotes(){
		var n = <%-JSON.stringify(notes)%>;
		return(n);
	}
</script>

<script src="/socket.io/socket.io.js"></script>

<% include footer.ejs %>